package quaternary.incorporeal.tile.decorative;

import net.minecraft.tileentity.TileEntityChest;

public class TileLivingwoodChest extends TileEntityChest {
	//This class really only exists so there's a class that's not TileEntityChest.
	//TESRs are of course bound per-tile, not per-block, so the game ended up assigning
	//the vanilla chest TESR to this not-vanilla chest.
	//And so, this class.
}
